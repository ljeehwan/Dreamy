<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.Dreamy.model.mapper.UserMapper">

	<select id="login" parameterType="Map" resultType="userDto">
		select uid, email, name, phone from User
		where email = #{email} and password = #{password}
	</select>

	<insert id="signup" parameterType="Map">
		insert into User (email, name, password, phone)
		values (#{email}, #{name}, #{password}, #{phone})
	</insert>

	<select id="getEmail" parameterType="String" resultType="Integer">
		select count(*) from User
		where email = #{email}
	</select>

	<select id="getName" parameterType="String" resultType="Integer">
		select count(*) from User
		where name = #{name}
	</select>

	<delete id="delete" parameterType="String">
		delete from User
		where email=#{email}
	</delete>

	<update id="update" parameterType="Map">
		update User set password=#{password}, phone=#{phone}
		where email=#{email}
	</update>
	
	<!-- 비밀번호 변경 관련 -->
	<select id="certification" parameterType="Map" resultType="int">
		select count(*)
		from User
		where email = #{email} and phone = #{phone}
	</select>
	
	<update id="updatePassword" parameterType="Map">
		update User 
		set	password=#{password} 
		where email=#{email}
	</update>
	<!-- 비밀번호 변경 관련 -->
	
</mapper>